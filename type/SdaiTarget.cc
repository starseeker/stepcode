#ifndef  TYPE_SDAITARGET_CC
#define  TYPE_SDAITARGET_CC

// This file was generated by exp2cxx,
// 0.9.1.
// You probably don't want to edit it since your modifications
// will be lost if exp2cxx is used to regenerate it.

#include "schema.h"
#include "type/SdaiTarget.h"


//////////  SELECT TYPE target

const TypeDescriptor * 
SdaiTarget::AssignEntity (SDAI_Application_instance * se)
{
  (void)se;
  // should never be here - done in Select class

   severity( SEVERITY_WARNING );
   std::cerr << __FILE__ << ":" << __LINE__ <<  ":  WARNING:  possible misuse of"
        << " SELECT TYPE from schema library.\n";
   Error( "Mismatch in underlying type." );
#ifdef __SUNCPLUSPLUS__
  std::cerr << se -> EntityName () << std::endl;
#endif
  return 0;
}

SDAI_Select * 
SdaiTarget::NewSelect ()
{
    SdaiTarget * tmp = new SdaiTarget();
    return tmp;
}


// STEP Part 21

void
SdaiTarget::STEPwrite_content (ostream& out, const char * currSch) const {
  (void)currSch;
      if (CurrentUnderlyingType () == flow_narrowing_test::t_set_representation_item) {
        _intaggregate -> STEPwrite (out);
        return;
    }
    if (CurrentUnderlyingType () == flow_narrowing_test::t_simple_int) {
        out <<  _integer;
        return;
    }
std::cerr << __FILE__ << ":" << __LINE__ <<  ":  ERROR in schema library:  \n" 
    << _POC_ << "\n\n";
}

BASE_TYPE
SdaiTarget::ValueType() const {
    if (CurrentUnderlyingType() == flow_narrowing_test::t_set_representation_item)
        return SET_TYPE;
    if (CurrentUnderlyingType() == flow_narrowing_test::t_simple_int)
        return sdaiINTEGER;
std::cerr << __FILE__ << ":" << __LINE__ <<  ":  ERROR in schema library:  \n" 
    << _POC_ << "\n\n";
    return (BASE_TYPE)0;
}

void
SdaiTarget::STEPwrite_verbose (ostream& out, const char *currSch) const
{
    const TypeDescriptor *td = CurrentUnderlyingType();
    std::string tmp;

    if ( td ) {
        // If we have a legal underlying type, get its name acc
        // to the current schema.
        StrToUpper( td->Name(currSch), tmp );
    }
    if (td == flow_narrowing_test::t_set_representation_item) {
        out << tmp << "(";
        _intaggregate->STEPwrite (out, currSch);
        out << ")";
        return;
    }
    if (td == flow_narrowing_test::t_simple_int) {
        out << tmp << "(" << _integer << ")";
        return;
    }
std::cerr << __FILE__ << ":" << __LINE__ <<  ":  ERROR in schema library:  \n" 
    << _POC_ << "\n\n";
    return;
}

Severity
SdaiTarget::STEPread_content (istream& in, InstMgrBase * instances,
            const char *utype, int addFileId, const char *currSch)
{
  (void)instances;
  (void)utype;
  (void)addFileId;
  (void)currSch;
      if (CurrentUnderlyingType () == flow_narrowing_test::t_set_representation_item) {
        _intaggregate->STEPread (in, &_error, flow_narrowing_test::t_set_representation_item -> AggrElemTypeDescriptor (),
          instances, addFileId, currSch);
        return severity ();
    }
    if (CurrentUnderlyingType () == flow_narrowing_test::t_simple_int) {
        ReadInteger (_integer, in, &_error, "),");
        return severity ();
    }

   severity( SEVERITY_WARNING );
   std::cerr << __FILE__ << ":" << __LINE__ <<  ":  WARNING:  possible misuse of"
        << " SELECT TYPE from schema library.\n";
   Error( "Mismatch in underlying type." );
    return severity ();
}

Severity
SdaiTarget::StrToVal_content (const char * str, InstMgrBase * instances)
{
  (void)str;
  (void)instances;
  switch (base_type)  {
  case SET_TYPE :  
    return _intaggregate->StrToVal (str, &_error, flow_narrowing_test::t_set_representation_item -> AggrElemTypeDescriptor ());
  default:  // should never be here - done in Select class

   severity( SEVERITY_WARNING );
   std::cerr << __FILE__ << ":" << __LINE__ <<  ":  WARNING:  possible misuse of"
        << " SELECT TYPE from schema library.\n";
   Error( "Mismatch in underlying type." );
#ifdef __SUNCPLUSPLUS__
std::cerr << str << "  " << instances << std::endl;
#endif
    return SEVERITY_WARNING;
  }
#ifdef __GNUG__

  return SEVERITY_NULL;
#endif
}

// STEP Part 22:  SDAI

    //  part 0
SdaiTarget::SdaiTarget( const SelectTypeDescriptor *typedescript )
  : SDAI_Select (typedescript)
{
#ifdef SC_LOGGING
    if( *logStream )
    {
    *logStream << "DAVE ERR entering SdaiTarget constructor." << std::endl;
    }
#endif
   _intaggregate = new IntAggregate;
   nullify();
#ifdef SC_LOGGING
    if( *logStream )
    {
//    *logStream << "DAVE ERR exiting SdaiTarget constructor." << std::endl;
    }
#endif
}

    //  part 1
SdaiTarget::SdaiTarget( const SdaiSet_representation_item_ptr& o,
                        const SelectTypeDescriptor *typedescript )
  : SDAI_Select (typedescript, flow_narrowing_test::t_set_representation_item)
{
#ifdef SC_LOGGING
    if( *logStream ) { *logStream << "DAVE ERR entering SdaiTarget constructor." << std::endl; }
#endif
   _intaggregate = new IntAggregate;
   _intaggregate->ShallowCopy (*o);
#ifdef SC_LOGGING
    if( *logStream ) { *logStream << "DAVE ERR exiting SdaiTarget constructor." << std::endl; }
#endif
}

SdaiTarget::SdaiTarget( const SdaiSimple_int& o,
                        const SelectTypeDescriptor *typedescript )
  : SDAI_Select (typedescript, flow_narrowing_test::t_simple_int)
{
#ifdef SC_LOGGING
    if( *logStream ) { *logStream << "DAVE ERR entering SdaiTarget constructor." << std::endl; }
#endif
   _integer = o;
#ifdef SC_LOGGING
    if( *logStream ) { *logStream << "DAVE ERR exiting SdaiTarget constructor." << std::endl; }
#endif
}

SdaiTarget::~SdaiTarget() {
   if( _intaggregate ) {
       delete _intaggregate;
       _intaggregate = 0;
    }
}

SdaiTarget_agg::SdaiTarget_agg( SelectTypeDescriptor *s)
  : SelectAggregate(), sel_type(s)
{
}

SdaiTarget_agg::~SdaiTarget_agg() { }


    //  part 2
SdaiTarget::operator SdaiSet_representation_item_ptr()
{
   if( CurrentUnderlyingType () == flow_narrowing_test::t_set_representation_item )
      return  _intaggregate;

   severity( SEVERITY_WARNING );
   Error( "Underlying type is not SdaiSet_representation_item_ptr" );

   severity( SEVERITY_WARNING );
   std::cerr << __FILE__ << ":" << __LINE__ <<  ":  WARNING:  possible misuse of"
        << " SELECT TYPE from schema library.\n";
   Error( "Mismatch in underlying type." );
   return 0;
}

SdaiTarget::operator SdaiSimple_int()
{
   if( CurrentUnderlyingType () == flow_narrowing_test::t_simple_int )
      return  _integer;

   severity( SEVERITY_WARNING );
   Error( "Underlying type is not SdaiSimple_int" );

   severity( SEVERITY_WARNING );
   std::cerr << __FILE__ << ":" << __LINE__ <<  ":  WARNING:  possible misuse of"
        << " SELECT TYPE from schema library.\n";
   Error( "Mismatch in underlying type." );
   return 0;
}


    //  part 3

    //  part 4
SdaiTarget& SdaiTarget::operator =( const SdaiSet_representation_item_ptr& o )
{
   nullify ();
   _intaggregate->ShallowCopy( *o );
   SetUnderlyingType( flow_narrowing_test::t_set_representation_item );
   return *this;
}

SdaiTarget& SdaiTarget::operator =( const SdaiSimple_int& o )
{
   nullify ();
   _integer = o;
   SetUnderlyingType( flow_narrowing_test::t_simple_int );
   return *this;
}


#ifndef COMPILER_DEFINES_OPERATOR_EQ

SdaiTarget& SdaiTarget::operator =( const SdaiTarget_ptr& o ) {
    SDAI_Select::operator=( *o );
    if ( o -> CurrentUnderlyingType() == flow_narrowing_test::t_set_representation_item ) {
        _intaggregate =  o -> _intaggregate;
        return *this;
    }
    if ( o -> CurrentUnderlyingType() == flow_narrowing_test::t_simple_int ) {
        _integer =  o -> _integer;
        return *this;
    }
    return *this;
}

SDAI_Select& SdaiTarget::operator =( const SDAI_Select& o ) {
    SDAI_Select::operator=( o );
    if ( o.CurrentUnderlyingType() == flow_narrowing_test::t_set_representation_item ) {
        _intaggregate = ( ( SdaiTarget& ) o )._intaggregate;
        return *this;
    }
    if ( o.CurrentUnderlyingType() == flow_narrowing_test::t_simple_int ) {
        _integer = ( ( SdaiTarget& ) o )._integer;
        return *this;
    }
   return *this;
}

#endif //ndef COMPILER_DEFINES_OPERATOR_EQ

    //  part 5
Logical SdaiTarget::IsSet_representation_item() const
{
   if( !exists() )
      return LUnknown;
   if( CurrentUnderlyingType () == flow_narrowing_test::t_set_representation_item )
      return LTrue;
   return LFalse;
}

Logical SdaiTarget::IsSimple_int() const
{
   if( !exists() )
      return LUnknown;
   if( CurrentUnderlyingType () == flow_narrowing_test::t_simple_int )
      return LTrue;
   return LFalse;
}

//////////  END SELECT TYPE SdaiTarget


void init_SdaiTarget( Registry& reg ) {
    std::string str;
        flow_narrowing_test::t_target = new SelectTypeDescriptor (
                  ~(0),        //unique elements,
                  "Target",        // Name
                  sdaiSELECT,        // FundamentalType
                  flow_narrowing_test::schema,        // Originating Schema
                  "SELECT (Set_Representation_Item, Simple_Int)",        // Description
                  (SelectCreator) create_SdaiTarget);        // Creator function
        flow_narrowing_test::schema->AddType(flow_narrowing_test::t_target);
    flow_narrowing_test::t_target -> Elements ().AddNode (flow_narrowing_test::t_set_representation_item);
    flow_narrowing_test::t_target -> Elements ().AddNode (flow_narrowing_test::t_simple_int);
    reg.AddType (*flow_narrowing_test::t_target);
}

#endif
