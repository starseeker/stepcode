#ifndef  ENTITY_SDAIFACE_SURFACE_CC
#define  ENTITY_SDAIFACE_SURFACE_CC

// This file was generated by exp2cxx,
// 0.9.1.
// You probably don't want to edit it since your modifications
// will be lost if exp2cxx is used to regenerate it.

#include "schema.h"
#include "entity/SdaiFace_surface.h"

EntityDescriptor * config_control_design::e_face_surface = 0;
AttrDescriptor * config_control_design::a_97face_geometry = 0;
AttrDescriptor * config_control_design::a_98same_sense = 0;

SdaiFace_surface::SdaiFace_surface(): SdaiFace(), _face_geometry( 0 ) {
        /*  parent: SdaiFace  */
        /* Ignore the first parent since it is */
        /* part of the main inheritance hierarchy */
        /*  parent: SdaiGeometric_representation_item  */
    HeadEntity(this);
    AppendMultInstance(new SdaiGeometric_representation_item(this));

    eDesc = config_control_design::e_face_surface;
    STEPattribute * a = new STEPattribute( * config_control_design::a_97face_geometry, ( SDAI_Application_instance_ptr * ) & _face_geometry );
    a->set_null();
    attributes.push( a );
    a = new STEPattribute( * config_control_design::a_98same_sense, & _same_sense );
    a->set_null();
    attributes.push( a );
    MakeDerived( "dim", "geometric_representation_item" );
}

SdaiFace_surface::SdaiFace_surface ( SdaiFace_surface & e ) : SdaiFace() {
    CopyAs( ( SDAI_Application_instance_ptr ) & e );
}

SdaiFace_surface::~SdaiFace_surface() {
}

SdaiFace_surface::SdaiFace_surface( SDAI_Application_instance * se, bool addAttrs ) : SdaiFace( se, addAttrs ), _face_geometry( 0 ) {
    /* Set this to point to the head entity. */
    HeadEntity(se);
        /* parent: SdaiFace */
        /* Ignore the first parent since it is part *
        ** of the main inheritance hierarchy        */
        /* parent: SdaiGeometric_representation_item */
    se->AppendMultInstance( new SdaiGeometric_representation_item( se, addAttrs ) );

    eDesc = config_control_design::e_face_surface;
    STEPattribute * a = new STEPattribute( * config_control_design::a_97face_geometry, ( SDAI_Application_instance_ptr * ) &_face_geometry );
        /* initialize to NULL (even if not optional)  */
    a ->set_null();
        /* Put attribute on this class' attributes list so the access functions still work. */
    attributes.push( a );
        /* Put attribute on the attributes list for the main inheritance hierarchy.  */
        /* The push method rejects duplicates found by comparing attrDescriptor's.   */
    if( addAttrs ) {
        se->attributes.push( a );
    }
    a = new STEPattribute( * config_control_design::a_98same_sense,  &_same_sense );
        /* initialize to NULL (even if not optional)  */
    a ->set_null();
        /* Put attribute on this class' attributes list so the access functions still work. */
    attributes.push( a );
        /* Put attribute on the attributes list for the main inheritance hierarchy.  */
        /* The push method rejects duplicates found by comparing attrDescriptor's.   */
    if( addAttrs ) {
        se->attributes.push( a );
    }
    MakeDerived( "dim", "geometric_representation_item" );
}


SdaiSurface_ptr SdaiFace_surface::face_geometry_() {
    if( !_face_geometry ) {
        _face_geometry = new SdaiSurface;
    }
    return (SdaiSurface_ptr) _face_geometry;
}

SdaiSurface_ptr_c SdaiFace_surface::face_geometry_() const {
    return (SdaiSurface_ptr) _face_geometry;
}

void SdaiFace_surface::face_geometry_( const SdaiSurface_ptr x ) {
    _face_geometry = x;
}

Boolean SdaiFace_surface::same_sense_() const {
    return (const Boolean) _same_sense;
}

void SdaiFace_surface::same_sense_( const Boolean x ) {
    _same_sense.put (x);
}

void init_SdaiFace_surface( Registry& reg ) {
    std::string str;

    config_control_design::e_face_surface->AddSupertype(config_control_design::e_face);
    config_control_design::e_face->AddSubtype(config_control_design::e_face_surface);
    config_control_design::e_face_surface->AddSupertype(config_control_design::e_geometric_representation_item);
    config_control_design::e_geometric_representation_item->AddSubtype(config_control_design::e_face_surface);
    config_control_design::a_97face_geometry =
      new AttrDescriptor( "face_geometry", config_control_design::e_surface, LFalse,
       LFalse, AttrType_Explicit, *config_control_design::e_face_surface);
        config_control_design::e_face_surface->AddExplicitAttr (config_control_design::a_97face_geometry);
        config_control_design::a_98same_sense =
          new AttrDescriptor("same_sense",t_sdaiBOOLEAN,
          LFalse,LFalse, AttrType_Explicit,
          *config_control_design::e_face_surface);
        config_control_design::e_face_surface->AddExplicitAttr (config_control_design::a_98same_sense);
        reg.AddEntity( *config_control_design::e_face_surface );
}

#endif
