#ifndef  ENTITY_SDAILOCALLY_REFINED_SPLINE_CURVE_CC
#define  ENTITY_SDAILOCALLY_REFINED_SPLINE_CURVE_CC

// This file was generated by exp2cxx,
// 0.9.1.
// You probably don't want to edit it since your modifications
// will be lost if exp2cxx is used to regenerate it.

#include "schema.h"
#include "entity/SdaiLocally_refined_spline_curve.h"

EntityDescriptor * ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve = 0;
AttrDescriptor * ap242_managed_model_based_3d_engineering_mim_lf::a_1405b_splines = 0;
AttrDescriptor * ap242_managed_model_based_3d_engineering_mim_lf::a_1406knot_values = 0;
AttrDescriptor * ap242_managed_model_based_3d_engineering_mim_lf::a_1407control_points_list = 0;
AttrDescriptor * ap242_managed_model_based_3d_engineering_mim_lf::a_1408scaling_factors = 0;
AttrDescriptor * ap242_managed_model_based_3d_engineering_mim_lf::a_1409closed_curve = 0;
AttrDescriptor * ap242_managed_model_based_3d_engineering_mim_lf::a_1410locally_refined_spline_type = 0;
AttrDescriptor * ap242_managed_model_based_3d_engineering_mim_lf::a_1411self_intersect = 0;
AttrDescriptor * ap242_managed_model_based_3d_engineering_mim_lf::a_1412domain = 0;

SdaiLocally_refined_spline_curve::SdaiLocally_refined_spline_curve(): SdaiBounded_curve(), _b_splines( 0 ), _knot_values( 0 ), _control_points_list( 0 ), _scaling_factors( 0 ), _domain( 0 ) {
        /*  parent: SdaiBounded_curve  */
        /* Ignore the first parent since it is */
        /* part of the main inheritance hierarchy */

    eDesc = ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve;
    _b_splines = new EntityAggregate;
    STEPattribute * a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1405b_splines, _b_splines );
    a->set_null();
    attributes.push( a );
    _knot_values = new RealAggregate;
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1406knot_values, _knot_values );
    a->set_null();
    attributes.push( a );
    _control_points_list = new EntityAggregate;
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1407control_points_list, _control_points_list );
    a->set_null();
    attributes.push( a );
    _scaling_factors = new RealAggregate;
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1408scaling_factors, _scaling_factors );
    a->set_null();
    attributes.push( a );
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1409closed_curve, & _closed_curve );
    a->set_null();
    attributes.push( a );
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1410locally_refined_spline_type, & _locally_refined_spline_type );
    a->set_null();
    attributes.push( a );
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1411self_intersect, & _self_intersect );
    a->set_null();
    attributes.push( a );
    _domain = new RealAggregate;
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1412domain, _domain );
    a->set_null();
    attributes.push( a );
    MakeDerived( "dim", "geometric_representation_item" );
}

SdaiLocally_refined_spline_curve::SdaiLocally_refined_spline_curve ( SdaiLocally_refined_spline_curve & e ) : SdaiBounded_curve() {
    CopyAs( ( SDAI_Application_instance_ptr ) & e );
}

SdaiLocally_refined_spline_curve::~SdaiLocally_refined_spline_curve() {
    delete _b_splines;
    delete _knot_values;
    delete _control_points_list;
    delete _scaling_factors;
    delete _domain;
}

SdaiLocally_refined_spline_curve::SdaiLocally_refined_spline_curve( SDAI_Application_instance * se, bool addAttrs ) : SdaiBounded_curve( se, addAttrs ), _b_splines( 0 ), _knot_values( 0 ), _control_points_list( 0 ), _scaling_factors( 0 ), _domain( 0 ) {
    /* Set this to point to the head entity. */
    HeadEntity(se);
        /* parent: SdaiBounded_curve */
        /* Ignore the first parent since it is part *
        ** of the main inheritance hierarchy        */

    eDesc = ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve;
    _b_splines = new EntityAggregate;
    STEPattribute * a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1405b_splines,  _b_splines );
        /* initialize to NULL (even if not optional)  */
    a ->set_null();
        /* Put attribute on this class' attributes list so the access functions still work. */
    attributes.push( a );
        /* Put attribute on the attributes list for the main inheritance hierarchy.  */
        /* The push method rejects duplicates found by comparing attrDescriptor's.   */
    if( addAttrs ) {
        se->attributes.push( a );
    }
    _knot_values = new RealAggregate;
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1406knot_values,  _knot_values );
        /* initialize to NULL (even if not optional)  */
    a ->set_null();
        /* Put attribute on this class' attributes list so the access functions still work. */
    attributes.push( a );
        /* Put attribute on the attributes list for the main inheritance hierarchy.  */
        /* The push method rejects duplicates found by comparing attrDescriptor's.   */
    if( addAttrs ) {
        se->attributes.push( a );
    }
    _control_points_list = new EntityAggregate;
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1407control_points_list,  _control_points_list );
        /* initialize to NULL (even if not optional)  */
    a ->set_null();
        /* Put attribute on this class' attributes list so the access functions still work. */
    attributes.push( a );
        /* Put attribute on the attributes list for the main inheritance hierarchy.  */
        /* The push method rejects duplicates found by comparing attrDescriptor's.   */
    if( addAttrs ) {
        se->attributes.push( a );
    }
    _scaling_factors = new RealAggregate;
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1408scaling_factors,  _scaling_factors );
        /* initialize to NULL (even if not optional)  */
    a ->set_null();
        /* Put attribute on this class' attributes list so the access functions still work. */
    attributes.push( a );
        /* Put attribute on the attributes list for the main inheritance hierarchy.  */
        /* The push method rejects duplicates found by comparing attrDescriptor's.   */
    if( addAttrs ) {
        se->attributes.push( a );
    }
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1409closed_curve,  &_closed_curve );
        /* initialize to NULL (even if not optional)  */
    a ->set_null();
        /* Put attribute on this class' attributes list so the access functions still work. */
    attributes.push( a );
        /* Put attribute on the attributes list for the main inheritance hierarchy.  */
        /* The push method rejects duplicates found by comparing attrDescriptor's.   */
    if( addAttrs ) {
        se->attributes.push( a );
    }
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1410locally_refined_spline_type,  &_locally_refined_spline_type );
        /* initialize to NULL (even if not optional)  */
    a ->set_null();
        /* Put attribute on this class' attributes list so the access functions still work. */
    attributes.push( a );
        /* Put attribute on the attributes list for the main inheritance hierarchy.  */
        /* The push method rejects duplicates found by comparing attrDescriptor's.   */
    if( addAttrs ) {
        se->attributes.push( a );
    }
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1411self_intersect,  &_self_intersect );
        /* initialize to NULL (even if not optional)  */
    a ->set_null();
        /* Put attribute on this class' attributes list so the access functions still work. */
    attributes.push( a );
        /* Put attribute on the attributes list for the main inheritance hierarchy.  */
        /* The push method rejects duplicates found by comparing attrDescriptor's.   */
    if( addAttrs ) {
        se->attributes.push( a );
    }
    _domain = new RealAggregate;
    a = new STEPattribute( * ap242_managed_model_based_3d_engineering_mim_lf::a_1412domain,  _domain );
        /* initialize to NULL (even if not optional)  */
    a ->set_null();
        /* Put attribute on this class' attributes list so the access functions still work. */
    attributes.push( a );
        /* Put attribute on the attributes list for the main inheritance hierarchy.  */
        /* The push method rejects duplicates found by comparing attrDescriptor's.   */
    if( addAttrs ) {
        se->attributes.push( a );
    }
    MakeDerived( "dim", "geometric_representation_item" );
}


EntityAggregate_ptr SdaiLocally_refined_spline_curve::b_splines_() {
    if( !_b_splines ) {
        _b_splines = new EntityAggregate;
    }
    return ( EntityAggregate_ptr ) _b_splines;
}

EntityAggregate_ptr_c SdaiLocally_refined_spline_curve::b_splines_() const {
    return ( EntityAggregate_ptr ) _b_splines;
}

void SdaiLocally_refined_spline_curve::b_splines_( const EntityAggregate_ptr x ) {
    if( !_b_splines ) {
        _b_splines = new EntityAggregate;
    }
    _b_splines->ShallowCopy( * x );
}

SdaiSpline_knot_values_ptr SdaiLocally_refined_spline_curve::knot_values_() {
    if( !_knot_values ) {
        _knot_values = new SdaiSpline_knot_values;
    }
    return ( SdaiSpline_knot_values_ptr ) _knot_values;
}

SdaiSpline_knot_values_ptr_c SdaiLocally_refined_spline_curve::knot_values_() const {
    return ( SdaiSpline_knot_values_ptr ) _knot_values;
}

void SdaiLocally_refined_spline_curve::knot_values_( const SdaiSpline_knot_values_ptr x ) {
    if( !_knot_values ) {
        _knot_values = new SdaiSpline_knot_values;
    }
    _knot_values->ShallowCopy( * x );
}

EntityAggregate_ptr SdaiLocally_refined_spline_curve::control_points_list_() {
    if( !_control_points_list ) {
        _control_points_list = new EntityAggregate;
    }
    return ( EntityAggregate_ptr ) _control_points_list;
}

EntityAggregate_ptr_c SdaiLocally_refined_spline_curve::control_points_list_() const {
    return ( EntityAggregate_ptr ) _control_points_list;
}

void SdaiLocally_refined_spline_curve::control_points_list_( const EntityAggregate_ptr x ) {
    if( !_control_points_list ) {
        _control_points_list = new EntityAggregate;
    }
    _control_points_list->ShallowCopy( * x );
}

RealAggregate_ptr SdaiLocally_refined_spline_curve::scaling_factors_() {
    if( !_scaling_factors ) {
        _scaling_factors = new RealAggregate;
    }
    return ( RealAggregate_ptr ) _scaling_factors;
}

RealAggregate_ptr_c SdaiLocally_refined_spline_curve::scaling_factors_() const {
    return ( RealAggregate_ptr ) _scaling_factors;
}

void SdaiLocally_refined_spline_curve::scaling_factors_( const RealAggregate_ptr x ) {
    if( !_scaling_factors ) {
        _scaling_factors = new RealAggregate;
    }
    _scaling_factors->ShallowCopy( * x );
}

Logical SdaiLocally_refined_spline_curve::closed_curve_() const {
    return (const Logical) _closed_curve;
}

void SdaiLocally_refined_spline_curve::closed_curve_( const Logical x ) {
    _closed_curve.put (x);
}

SdaiLocally_refined_spline_type_enum_var SdaiLocally_refined_spline_curve::locally_refined_spline_type_() {
    return (Locally_refined_spline_type_enum) _locally_refined_spline_type;
}

const SdaiLocally_refined_spline_type_enum_var SdaiLocally_refined_spline_curve::locally_refined_spline_type_() const {
    return (const Locally_refined_spline_type_enum) _locally_refined_spline_type;
}

void SdaiLocally_refined_spline_curve::locally_refined_spline_type_( const SdaiLocally_refined_spline_type_enum_var x ) {
    _locally_refined_spline_type.put( x );
}

Logical SdaiLocally_refined_spline_curve::self_intersect_() const {
    return (const Logical) _self_intersect;
}

void SdaiLocally_refined_spline_curve::self_intersect_( const Logical x ) {
    _self_intersect.put (x);
}

RealAggregate_ptr SdaiLocally_refined_spline_curve::domain_() {
    if( !_domain ) {
        _domain = new RealAggregate;
    }
    return ( RealAggregate_ptr ) _domain;
}

RealAggregate_ptr_c SdaiLocally_refined_spline_curve::domain_() const {
    return ( RealAggregate_ptr ) _domain;
}

void SdaiLocally_refined_spline_curve::domain_( const RealAggregate_ptr x ) {
    if( !_domain ) {
        _domain = new RealAggregate;
    }
    _domain->ShallowCopy( * x );
}

void init_SdaiLocally_refined_spline_curve( Registry& reg ) {
    std::string str;

    ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve->_where_rules = new Where_rule__list;
        Where_rule * wr;
        str.clear();
        str.append( "wr1: (" );
    str.append( "SIZEOF( b_splines ) = SIZEOF( control_points_list )\n" );
        str.append( ");\n" );
        wr = new Where_rule( str.c_str() );
        ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve->_where_rules->Append( wr );
        str.clear();
        str.append( "wr2: (" );
    str.append( "SIZEOF( scaling_factors ) = SIZEOF( control_points_list )\n" );
        str.append( ");\n" );
        wr = new Where_rule( str.c_str() );
        ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve->_where_rules->Append( wr );
        str.clear();
        str.append( "wr3: (" );
    str.append( "constraints_scaling( scaling_factors )\n" );
        str.append( ");\n" );
        wr = new Where_rule( str.c_str() );
        ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve->_where_rules->Append( wr );
    ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve->AddSupertype(ap242_managed_model_based_3d_engineering_mim_lf::e_bounded_curve);
    ap242_managed_model_based_3d_engineering_mim_lf::e_bounded_curve->AddSubtype(ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve);
        ListTypeDescriptor * t_327 = new ListTypeDescriptor;
        t_327->AssignAggrCreator((AggregateCreator) create_EntityAggregate);        // Creator function
        t_327->SetBound1( 2 );
        t_327->SetBound2( 2147483647 );
        t_327->FundamentalType(LIST_TYPE);
        t_327->Description("LIST [2:?] OF local_b_spline");
        t_327->OriginatingSchema(ap242_managed_model_based_3d_engineering_mim_lf::schema);
        t_327->ReferentType(ap242_managed_model_based_3d_engineering_mim_lf::e_local_b_spline);
        ap242_managed_model_based_3d_engineering_mim_lf::schema->AddUnnamedType(t_327);
        ap242_managed_model_based_3d_engineering_mim_lf::a_1405b_splines =
          new AttrDescriptor("b_splines",t_327,LFalse,LFalse, AttrType_Explicit,
          *ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve);
        ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve->AddExplicitAttr (ap242_managed_model_based_3d_engineering_mim_lf::a_1405b_splines);
        ap242_managed_model_based_3d_engineering_mim_lf::a_1406knot_values =
          new AttrDescriptor("knot_values",ap242_managed_model_based_3d_engineering_mim_lf::t_spline_knot_values,
          LFalse,LFalse, AttrType_Explicit,
          *ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve);
        ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve->AddExplicitAttr (ap242_managed_model_based_3d_engineering_mim_lf::a_1406knot_values);
        ListTypeDescriptor * t_328 = new ListTypeDescriptor;
        t_328->AssignAggrCreator((AggregateCreator) create_EntityAggregate);        // Creator function
        t_328->SetBound1( 2 );
        t_328->SetBound2( 2147483647 );
        t_328->FundamentalType(LIST_TYPE);
        t_328->Description("LIST [2:?] OF cartesian_point");
        t_328->OriginatingSchema(ap242_managed_model_based_3d_engineering_mim_lf::schema);
        t_328->ReferentType(ap242_managed_model_based_3d_engineering_mim_lf::e_cartesian_point);
        ap242_managed_model_based_3d_engineering_mim_lf::schema->AddUnnamedType(t_328);
        ap242_managed_model_based_3d_engineering_mim_lf::a_1407control_points_list =
          new AttrDescriptor("control_points_list",t_328,LFalse,LFalse, AttrType_Explicit,
          *ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve);
        ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve->AddExplicitAttr (ap242_managed_model_based_3d_engineering_mim_lf::a_1407control_points_list);
        ListTypeDescriptor * t_329 = new ListTypeDescriptor;
        t_329->AssignAggrCreator((AggregateCreator) create_RealAggregate);        // Creator function
        t_329->SetBound1( 2 );
        t_329->SetBound2( 2147483647 );
        t_329->FundamentalType(LIST_TYPE);
        t_329->Description("LIST [2:?] OF REAL");
        t_329->OriginatingSchema(ap242_managed_model_based_3d_engineering_mim_lf::schema);
        t_329->ReferentType(t_sdaiREAL);
        ap242_managed_model_based_3d_engineering_mim_lf::schema->AddUnnamedType(t_329);
        ap242_managed_model_based_3d_engineering_mim_lf::a_1408scaling_factors =
          new AttrDescriptor("scaling_factors",t_329,LFalse,LFalse, AttrType_Explicit,
          *ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve);
        ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve->AddExplicitAttr (ap242_managed_model_based_3d_engineering_mim_lf::a_1408scaling_factors);
        ap242_managed_model_based_3d_engineering_mim_lf::a_1409closed_curve =
          new AttrDescriptor("closed_curve",t_sdaiLOGICAL,
          LFalse,LFalse, AttrType_Explicit,
          *ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve);
        ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve->AddExplicitAttr (ap242_managed_model_based_3d_engineering_mim_lf::a_1409closed_curve);
        ap242_managed_model_based_3d_engineering_mim_lf::a_1410locally_refined_spline_type =
          new AttrDescriptor("locally_refined_spline_type",ap242_managed_model_based_3d_engineering_mim_lf::t_locally_refined_spline_type_enum,
          LFalse,LFalse, AttrType_Explicit,
          *ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve);
        ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve->AddExplicitAttr (ap242_managed_model_based_3d_engineering_mim_lf::a_1410locally_refined_spline_type);
        ap242_managed_model_based_3d_engineering_mim_lf::a_1411self_intersect =
          new AttrDescriptor("self_intersect",t_sdaiLOGICAL,
          LFalse,LFalse, AttrType_Explicit,
          *ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve);
        ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve->AddExplicitAttr (ap242_managed_model_based_3d_engineering_mim_lf::a_1411self_intersect);
        ListTypeDescriptor * t_330 = new ListTypeDescriptor;
        t_330->AssignAggrCreator((AggregateCreator) create_RealAggregate);        // Creator function
        t_330->SetBound1( 2 );
        t_330->SetBound2( 2 );
        t_330->FundamentalType(LIST_TYPE);
        t_330->Description("LIST [2:2] OF REAL");
        t_330->OriginatingSchema(ap242_managed_model_based_3d_engineering_mim_lf::schema);
        t_330->ReferentType(t_sdaiREAL);
        ap242_managed_model_based_3d_engineering_mim_lf::schema->AddUnnamedType(t_330);
        ap242_managed_model_based_3d_engineering_mim_lf::a_1412domain =
          new AttrDescriptor("domain",t_330,LFalse,LFalse, AttrType_Explicit,
          *ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve);
        ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve->AddExplicitAttr (ap242_managed_model_based_3d_engineering_mim_lf::a_1412domain);
        reg.AddEntity( *ap242_managed_model_based_3d_engineering_mim_lf::e_locally_refined_spline_curve );
}

#endif
